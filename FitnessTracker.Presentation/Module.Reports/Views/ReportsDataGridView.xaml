<UserControl x:Class="FitnessTracker.Presentation.Module.Reports.Views.ReportsDataGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:FitnessTracker.Presentation.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <controls:LoadingSpinner IsLoading="{ Binding Path=ReportsProvider.IsLoading }" Diameter="45" Thickness="5" />

        <Grid>
            <Grid.Style>
                <Style TargetType="{ x:Type Grid }">
                    <Style.Triggers>
                        <DataTrigger Binding="{ Binding Path=ReportsProvider.IsLoading }" Value="True">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                        <DataTrigger Binding="{ Binding Path=ReportsProvider.IsLoading }" Value="False">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            
            <DockPanel Grid.Row="0" LastChildFill="False">
                <Button DockPanel.Dock="Left" MinWidth="60" Content="Add" />
                <Button DockPanel.Dock="Left" MinWidth="60" Content="Delete" />
            </DockPanel>

            <DataGrid Grid.Row="1" ItemsSource="{ Binding Path=ReportsProvider.Data }" />
        </Grid>
    </Grid>
</UserControl>
